<div class="container">
  <div class="row cards">
    <% @lists.each do |list| %>   
    <h4><%= link_to list.name, list_path(list)%></h4>
    <% end %>
  </div>
</div>
<br>
<br>
<div class="container">
  <div class="row cards">
  <h1><%= link_to "create_new_list", new_list_path %></h1>
  </div>
</div>